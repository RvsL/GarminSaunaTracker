# 🖥️ Руководство по работе с симулятором Garmin Connect IQ

## 🚀 Запуск симулятора

### Способ 1: Автоматический (рекомендуется)
```bash
./run_simulator_gui.sh
```

### Способ 2: Пошаговый (если автоматический не работает)

#### Шаг 1: Откройте симулятор
```bash
open "/Users/rvsl/Library/Application Support/Garmin/ConnectIQ/Sdks/connectiq-sdk-mac-8.4.0-2025-12-03-5122605dc/bin/ConnectIQ.app"
```

Дождитесь полной загрузки симулятора (10-15 секунд)

#### Шаг 2: Настройте симулятор
В окне симулятора:
1. **Выберите устройство**: В выпадающем списке найдите и выберите `Instinct 2S`
2. **Дождитесь инициализации**: Экран часов должен стать активным (не серым)

#### Шаг 3: Загрузите приложение
В новом терминале выполните:
```bash
export JAVA_HOME="/opt/homebrew/opt/openjdk@17"
export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"
monkeydo bin/SaunaTracker.prg instinct2s
```

## 🎮 Управление в симуляторе

### Интерфейс симулятора
- **Экран часов**: В центре окна - изображение часов Instinct 2S
- **Кнопки**: Вокруг экрана расположены кликабельные области кнопок
- **Меню**: Сверху - меню с опциями симуляции

### Кнопки на Instinct 2S
```
    [LIGHT]          [GPS/START]
        ╔═══════════╗
        ║           ║
        ║   ЭКРАН   ║
        ║           ║
        ╚═══════════╝
    [MENU]           [BACK]
```

Для вашего приложения:
- **GPS/START** (верхняя правая) - начать/переключить режим (сауна ↔ перерыв)
- **BACK** (нижняя правая) - завершить сессию

### Как кликать по кнопкам
1. Наведите курсор на область кнопки на изображении часов
2. Кликните мышкой
3. Вы увидите визуальную реакцию и изменения в приложении

## 🔧 Возможные проблемы и решения

### Серый экран в симуляторе
**Решение 1**: В меню симулятора выберите `Simulation → Start Simulation`

**Решение 2**: Кликните дважды по экрану часов

**Решение 3**: Закройте симулятор и запустите заново через GUI скрипт

### "Unable to connect to simulator"
**Причина**: Симулятор не успел полностью загрузиться

**Решение**:
1. Убедитесь, что симулятор открыт
2. Подождите 15-20 секунд после открытия
3. Повторите команду `monkeydo`

### Приложение не загружается
**Решение**:
1. Проверьте, что выбрано правильное устройство (Instinct 2S)
2. Перезапустите симулятор
3. Пересоберите проект:
```bash
./run_simulator_gui.sh
```

### Java ошибки
**Решение**:
```bash
# Проверьте Java
java -version

# Должно показать: openjdk version "17.0.17"
# Если нет - перезапустите терминал или выполните:
source ~/.zshrc
```

## 🎯 Тестирование приложения SaunaTracker

### Сценарий 1: Первый запуск
1. Нажмите **START** → Должна начаться сессия
2. На экране: 
   - Таймер: `15:00`
   - Статус: `SAUNA #1`
   - Время дня (вверху)
   - Пульс (справа вверху)

### Сценарий 2: Переключение режимов
1. Нажмите **START** еще раз → Перерыв
2. Статус изменится на `REST`
3. Таймер сбросится в `0:00`
4. Нажмите **START** снова → Новый раунд
5. Статус: `SAUNA #2`
6. Таймер: `15:00`

### Сценарий 3: Таймер
1. Подождите пока таймер отсчитывает время
2. Когда таймер достигнет `0:00` - должна сработать вибрация (в симуляторе - визуальный эффект)

### Сценарий 4: Завершение
1. Нажмите **BACK** → Сессия завершается
2. Приложение сохраняет FIT активность
3. Приложение закрывается

## 📊 Что отображается на экране

```
┌─────────────────┐
│    12:34     ❤️98│  ← Время дня и пульс
│                 │
│                 │
│     14:52       │  ← ТАЙМЕР (обратный отсчет)
│                 │
│   SAUNA #2      │  ← Статус (SAUNA/REST) и раунд
│                 │
│ 36.8C | 45 min  │  ← Температура | Общее время
└─────────────────┘
```

## 💡 Полезные функции симулятора

### Меню Simulation
- **Start/Stop Simulation** - запуск/остановка симуляции
- **Reload** - перезагрузить приложение
- **Screenshot** - сделать скриншот

### Меню Settings
- **Display** - настройки отображения
- **Sensors** - симуляция датчиков (пульс, температура)

### Симуляция датчиков
1. Откройте `Settings → Sensors`
2. Установите значения:
   - Heart Rate: 60-180 bpm
   - Temperature: 35-40°C
3. Эти значения отобразятся в приложении

## 📝 Горячие клавиши

- `Cmd + R` - Перезагрузить приложение
- `Cmd + Q` - Закрыть симулятор
- `Cmd + S` - Скриншот

## 🔄 Быстрый перезапуск

Если нужно быстро протестировать изменения в коде:

```bash
# Один скрипт делает всё
./run_simulator_gui.sh
```

Или в VS Code:
1. `Cmd + Shift + P`
2. `Monkey C: Run`
3. Выбрать `instinct2s`
